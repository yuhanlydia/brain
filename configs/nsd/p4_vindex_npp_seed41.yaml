method: NPP-OPSD
trainer:
  alpha_scale: 1.0
  alpha_max: 1.0
backend: vindex
task: nsd_vqa
hardware_profile: configs/hardware/gpu16gb.yaml
paths:
  vindex_checkout:
  model:
  data:
  manifest:
experiment:
  phase: P4
  purpose: npp_opsd_evaluation
  seed: 41
  subjects: [subj01, subj02, subj05, subj07]
  output_dir: outputs/p4_vindex_npp_seed41
  methods: [npp-opsd]
  controls: [correct, shuffled, zero, covariance-noise, wrong-subject]
candidate_count: 4
micro_batch_size: 1
generation:
  max_new_tokens: 64
model:
  size: 7B
  quantization: 4-bit
  frozen: true
trainable:
  projector: true
  lora: true
