method: NPP-OPSD
trainer:
  alpha_scale: 1.0
  alpha_max: 1.0
backend: vindex
task: nsd_vqa
hardware_profile: configs/hardware/gpu16gb.yaml
paths:
  vindex_checkout:
  model:
  data:
  manifest:
candidate_count: 4
micro_batch_size: 1
generation:
  max_new_tokens: 64
model:
  size: 7B
  quantization: 4-bit
  frozen: true
trainable:
  projector: true
  lora: true
