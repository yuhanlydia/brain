method: benchmark_matrix
backend: vindex
task: nsd_vqa
hardware_profile: configs/hardware/gpu16gb.yaml
paths:
  vindex_checkout:
  model:
  data:
  manifest:
experiment:
  phase: P2
  purpose: baseline_reproduction
  seed: 41
  subjects: [subj01, subj02, subj05, subj07]
  output_dir: outputs/p2_vindex_baselines_seed41
  methods:
    - ce
    - exact-image-opsd
    - map-image-opsd
    - uniform-mixture
    - plain-posterior-mixture
    - credit-style
    - dapd
    - vad-style
  controls: [correct, shuffled, zero, covariance-noise, wrong-subject]
candidate_count: 4
micro_batch_size: 1
generation:
  max_new_tokens: 64
model:
  size: 7B
  quantization: 4-bit
  frozen: true
trainable:
  projector: true
  lora: true
